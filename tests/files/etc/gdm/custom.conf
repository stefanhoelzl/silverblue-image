# Autologin, so the VM reaches a real user session rather than stopping at the greeter.
# uresourced's MemoryMin lands on user-1000.slice and both notify-* scripts need a
# session bus, so without a logged-in user none of that is observable.
[daemon]
AutomaticLoginEnable=True
AutomaticLogin=tester
