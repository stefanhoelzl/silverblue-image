# Empty on purpose: no zram device. zswap in front of the disk swapfile replaces it - see
# /usr/lib/tmpfiles.d/zswap.conf. An empty file here is the opt-out that
# zram-generator-defaults documents in /usr/lib/systemd/zram-generator.conf.
